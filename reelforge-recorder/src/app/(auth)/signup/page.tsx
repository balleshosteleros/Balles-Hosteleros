import { SignupForm } from "@/features/auth/components/SignupForm";

export const metadata = { title: "Crear cuenta — ReelForge Recorder" };

export default function SignupPage() {
  return <SignupForm />;
}
