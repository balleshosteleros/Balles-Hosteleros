# Handoff RRHH consolidacion - 2026-05-20

## Estado de cierre

Sesion cerrada con un primer bloque de consolidacion RRHH implementado y una descomposicion formal de continuidad en `docs/rrhh-consolidacion/`.

Branch actual: `main`
Repo: `https://github.com/balleshosteleros/Balles-Hosteleros.git`

## Que se ha realizado

### Runtime RRHH

- Se sustituyo `/rrhh` vacio por un hub operativo real en `src/app/(main)/rrhh/page.tsx`.
- Se creo `src/features/rrhh/actions/dashboard-actions.ts` con `getRrhhDashboard()`.
- El dashboard lee empresa activa, empleados activos, altas recientes, fichajes abiertos, firmas pendientes, solicitudes pendientes, alertas y altas recientes.
- La ficha de empleado en `src/app/(main)/rrhh/empleados/[id]/page.tsx` ahora carga datos reales adicionales:
  - perfil y empresas de acceso
  - fichajes por empleado
  - solicitudes por empleado
  - horario/patron activo
  - firmas existentes
- Se anadieron lecturas reales:
  - `listFichajesEmpleado(empleadoId, rango?)`
  - `listSolicitudesEmpleado(empleadoId)`
  - `getEmpleadoHorarioActual(empleadoId)`
- Se corrigio `getMiInformacionLaboral()` para consultar por `empleados.user_id`, no por `profile_id`.

### Fichajes e incidencias

- Se corrigio `updateFichaje()` para escribir en `observaciones`, no en una columna inexistente `notas`.
- Se corrigio el autocierre de fichajes huerfanos en `mi-panel`.
- Se corrigio el cron `src/app/api/cron/cerrar-fichajes-huerfanos/route.ts`.
- Decision fijada: `fichajes.estado` debe respetar la constraint real de `056_fichajes.sql`; las incidencias viven en `fichajes.incidencia`, no como estado persistido.
- `FichajesView` detecta incidencias por campo `incidencia`.
- `src/features/rrhh/data/fichajes.ts` acepta estados reales de BD: `pendiente`, `trabajando`, `pausa`, `completado`.

### Planificacion formal

Se genero el paquete documental definitivo en `docs/rrhh-consolidacion/`:

- `EXECUTION_PLAN.md`
- `TASK-001-cierre-bloque-runtime-iniciado.md`
- `Full-TASK-001-cierre-bloque-runtime-iniciado.md`
- `TASK-002-empleados-y-fichajes-canonicos.md`
- `Full-TASK-002-empleados-y-fichajes-canonicos.md`
- `TASK-003-horarios-y-solicitudes-discovery.md`
- `Full-TASK-003-horarios-y-solicitudes-discovery.md`
- `TASK-004-firmas-hardening-operativo.md`
- `Full-TASK-004-firmas-hardening-operativo.md`
- `TASK-005-reclutamiento-a-empleado.md`
- `Full-TASK-005-reclutamiento-a-empleado.md`
- `TASK-006-accesos-apps-exclusion-y-remediacion.md`
- `Full-TASK-006-accesos-apps-exclusion-y-remediacion.md`

El corte definitivo separa:

- `TASK-001`: cierre del bloque runtime ya iniciado.
- `TASK-002`: nucleo canonico empleados + fichajes + mi-panel.
- `TASK-003`: discovery previo de horarios y solicitudes.
- `TASK-004`: hardening operativo de firmas.
- `TASK-005`: reclutamiento a empleado.
- `TASK-006`: exclusion/remediacion separada de accesos apps.

## Validaciones ejecutadas

- `npm run typecheck`: pasa.
- `npm run build`: pasa.

Avisos no bloqueantes observados en build:

- `metadataBase` no configurado; Next usa `http://localhost:3000`.
- Edge runtime desactiva static generation en paginas afectadas.

Nota de tooling:

- `npm run build` modifica `next-env.d.ts` de `.next/dev/types/routes.d.ts` a `.next/types/routes.d.ts`.
- Ese cambio se considero ruido generado y se restauro antes del commit.

## Donde empezar al retomar

Ruta principal de reentrada:

1. Leer este handoff.
2. Leer `docs/rrhh-consolidacion/EXECUTION_PLAN.md`.
3. Si se quiere revisar lo ya hecho: empezar por `docs/rrhh-consolidacion/Full-TASK-001-cierre-bloque-runtime-iniciado.md`.
4. Si se quiere continuar construyendo: empezar por `docs/rrhh-consolidacion/Full-TASK-002-empleados-y-fichajes-canonicos.md`.

Recomendacion operativa:

- No entrar todavia en runtime profundo de horarios, firmas o reclutamiento.
- Primero cerrar `TASK-002`.
- Despues ejecutar `TASK-003` como discovery antes de tocar horarios/solicitudes.
- Ejecutar `TASK-004` antes de declarar firmas como consolidado.
- Mantener `TASK-006` fuera del hilo principal salvo peticion explicita de remediacion de seguridad.

## Riesgos pendientes

- La ficha aun conserva fallback heredado en algunos tabs.
- `src/features/rrhh/data/rrhh.ts` sigue existiendo como dataset legacy/mock; no usarlo como fuente funcional.
- Hay acciones de horarios que apuntan a tablas que deben confirmarse en migraciones:
  - `rrhh_turnos`
  - `rrhh_cuadrantes`
  - `rrhh_descansos`
- Reclutamiento espera `candidaturas_rate_limit` y bucket `cvs-candidatos`; confirmar versionado/entorno antes de smoke.
- No se hizo smoke manual en navegador con usuario real.

## Checkpoint recomendado

Este estado es buen punto de commit y rollback:

- runtime RRHH inicial validado por typecheck/build
- documentos `TASK`/`Full-TASK` listos
- siguiente trabajo claramente delimitado

